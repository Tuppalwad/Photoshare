import { StyleSheet, Text, View } from 'react-native'
import React ,{useContext}from 'react'
import {Context} from 'Context'
const AuthoAction = () => {
    const {authAction} = useContext(Context)
  return (
    <View>
      <Text>

      </Text>
    </View>
  )
}

export default AuthoAction

const styles = StyleSheet.create({})